package collections

fun main() {
    var list = listOf<String>("Kotlin", "Java", "C#", "Javascript")
   // list[2] = "C++" -> immutable olduğu için eleman değiştirilemez, silinemez, eklenemez.

   // for (item in list) println(item)

    // for (item in list.indices) println(list[item])
}